/**
 * Help overlay (`?`).
 *
 * Lists the bindings of the focused pane (from the resolved keymap) plus the
 * global ones. Closes on Esc / `?`.
 *
 * TODO: implement as an overlay component.
 */

export {};
