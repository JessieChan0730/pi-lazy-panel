/**
 * Session Info dialog (`i` in the sessions pane).
 *
 *   ┌──────────────────────────────────────┐
 *   │           SESSION INFO               │
 *   │ Name       FilmRecall                │
 *   │ Model      claude-opus-4             │
 *   │ Messages   128                       │
 *   │ Tokens     84.2k                     │
 *   │ Cost       $1.42                     │
 *   │ Created    Sep 20 22:18              │
 *   │ Updated    Sep 20 22:21              │
 *   │ Path       ~/.pi/sessions/...        │
 *   │                         [Esc] Close  │
 *   └──────────────────────────────────────┘
 *
 * `y` copies the whole dialog text to the clipboard.
 *
 * TODO: implement as an overlay component fed by data/content.ts#loadSessionInfo.
 */

export {};
