/**
 * Search bar (lazygit-style).
 *
 * Appears in the footer row when the user presses `/`:
 *   搜索: <typed text>
 * Enter runs the search against the focused pane only, Esc cancels.
 * Results are highlighted in place; n / N jump between matches.
 *
 * TODO: implement (likely wrapping pi-tui Input).
 */

export {};
