/**
 * Sessions pane (left, top).
 *
 * Lists `SessionRow[]`. Shows name / time / model / cwd / preview in a compact
 * form. Supports cursor movement, multi-select (<space>), sorting and scope
 * toggle. Selection changes drive the tree pane and the content pane.
 *
 * TODO: implement as a pi-tui Component.
 */

// TODO: export class SessionsPane implements Component { ... }
export {};
