/**
 * Content pane (right, full height, read-only).
 *
 * Renders `ContentBlock[]` as boxed messages ("YOU · 22:18" / "ASSISTANT · 22:18")
 * using pi's native Markdown renderer from @earendil-works/pi-tui. Provides a
 * vim-like cursor (h/j/k/l, b/e, gg/G, zz), yank (y/yy), preview mode (v) and
 * search highlighting with n/N.
 *
 * TODO: implement as a pi-tui Component (likely wrapping ScrollView + Markdown).
 */

// TODO: export class ContentPane implements Component { ... }
export {};
