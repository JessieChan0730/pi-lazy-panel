## 问题描述

目前 pi 启动的 resume 感觉操作很别扭，方向键控制上下移动，然后 ctrl + d 删， 能否帮我做一个插件 lazygit 那种（hjkl, d 是删除，r 是 rename）。操作 pi resume 的插件啊？

## 项目描述

一个类似 lazygit 的插件，用来管理 pi 的历史对话，主要命令有三个 /resume（恢复会话，从历史会话中选择并恢复）  /tree  （会话树导航，跳转到对话树的任意节点继续对话，某个对话详情里面的某一个界面） /new （新建一个对话），然后还有围绕着对话进行操作的命令：/name（命名会话，为当前会话设置一个易读的名称） /session（会话信息，显示会话文件路径、ID、消息数、token 用量和费用） /fork（分叉会话，从之前的用户消息分叉出一个新会话文件）/clone（克隆会话复制当前活动分支到新会话文件）/copy （复制 AI 最后一条回复到剪贴板）/export（导出会话导出为 HTML 或 JSONL 文件）/import（导入会话从 JSONL 文件导入并恢复会话）
/share（分享会话上传为私有 GitHub Gist，生成可分享的 HTML 链接）， 根据这些命令能够拿到信息，我觉得产品可以设计成为这样：

- 左边第一个面板是显示历史对话, 也就是 /resume 里面的那些数据
- 左边第二个面板（在第一个面板底部）是显示对话节点 也就是 /tree 里面的那些数据（根据上面选择的不同的 /resume 来确自动变化这里面的内容）
- 右边第一个面板是显示对话的详细内容（需要区分是AI的消息，还是我的消息），这个面板占据全部高度
- 搜索，放在底部不需要一个输入框。当用户输入 / 时候，底部出现 搜索：两个字样，随后用户输入内容回车进行搜索（只搜索当前focus面板中的内容），这个完全参考 lazygit 就行

聚焦到得不同的面板，不同的快捷键有不同的含义。

```txt
关系其实是：
    resume1
        - tree1
            - node1
            - node2
```

也就是随着第一个面板，用户选择聚焦不同的resume,下面的第二个面板的tree也要不停变化显示不同的可还原的节点

暂定的指定（后续可能会适配）：
/compact，/settings，/changelog

确定不会用到的指令：
/login，/logout，/model，/thinking，/scoped-models

## 使用流程

用户安装插件之后，使用 /lazy-history 命令可以打开一个 tui 对话框，整体布局是参考项目描述

- ~~打开时 SESSIONS 光标自动落到 pi 当前打开的会话上，TREE / CONTENT 随之显示当前对话；新对话（还没有会话文件或还没列出来）则选中第一行~~（2026-09-22 完成；C / A 切换范围后仍回到顶部）

## 快捷键操作

### 通用的快捷键

~~h/l： 参考 lazygit，h 切换到上一个面板，l 切换到下一个面板（tab 也保留为“下一个”），聚焦在不同的面板上，快捷键会有不同的表现~~
~~1/2/3： 每个面板标题前有数字标记（[1] SESSIONS、[2] TREE、[3] CONTENT），按下对应数字直接跳到该面板~~
~~C：切换到 Current Folder 模式（只做单向切换，已经是 Current 时按下无效果）~~
~~A：切换到 All 模式（只做单向切换，已经是 All 时按下无效果）~~

- ~~SESSIONS 面板标题右侧始终显示 `1/15 · Current · recent`（位置 · 模式 · 排序），C/A 两种模式都要有；宽度不够时按 `1/15 · Current` → `1/15 · Cur` → `1/15` 逐级缩短，不能整段消失~~
- ~~底部 footer 只提示能切换过去的那个：当前是 Current 就显示 `A All`，当前是 All 就显示 `C Current`~~
~~?: 快捷键提示菜单, 不同的面板提示不同的快捷键。~~
~~exit/q： 退出插件~~
~~/: 底部出现 搜索：两个字样，随后用户输入内容回车进行搜索，只搜索当前fouce的,文本需要高亮，使用 n/N 可以上一个和下一个，具体参考lazygit的形式~~（2026-09-23 完成：三个面板各记各的关键字，输入时实时跳到原位置之后的第一个匹配，Enter 保留、Esc 取消并回到原位置；n / N 回绕；标题右侧 `2/7 matches`，footer 显示关键字和 n / N / Esc 提示；命中文字高亮、当前匹配反色；列表不过滤只跳转）

### 左侧列表第一个面板（resume）

~~/: 底部出现 搜索：两个字样，随后用户输入内容回车进行搜索，只搜索当前fouce的,文本需要高亮，使用 n/N 可以上一个和下一个，具体参考lazygit的形式~~（2026-09-23 完成：普通词只按 名称 / 首条消息预览 匹配，模型和路径只通过 `model:` / `path:` 匹配（`path:` 也认 `~/…` 写法），`name:` 只看会话名，`after:` / `before:` 看最后更新时间，`tag:` 忽略；搜索生效期间 n / N 是上下一个匹配，n 不再是新建会话）
~~j/k/方向上下键盘：上下移动~~
d: 删除对话
r: 重命名对话(类似于进入 /resume 之后使用 ctrl + r) 修改过名字的 sessiion 面板顶部显示名字，并且看通过高亮还是什么手段，告诉用户这个对话修改过名称
o: Fork the session and open the fork
y: 克隆会话 复制当前活动分支到新会话文件
~~J/K：上下滚动右侧的聊天内容~~
~~gg: 回到列表顶部~~
~~G: 跳转到列表底部~~
space: 多选聊天，主要是为了批量删除，fork 和 rname 不可用，如果选中多个按下了 f/r 提示用户：不能对多个对像进行操作
E: 导出对话，点击快捷键之后，显示一个输入框输入输出的目录
I: 导入对话，点击快捷键之后，显示一个输入框输入导入的目录
S: 分享会话 上传为私有 GitHub Gist，生成可分享的 HTML 链接，这里用大写的S, 避免和排序冲突
Y: 复制回复 复制 AI 最后一条回复到剪贴板
s: 对列表排序：（Threaded， Recent， Fuzzy）
~~enter：点击恢复这个对话~~（对应 /resume：切过去并关闭面板，失败原因留在 footer）
n: 新建一个对话，显示输入框，必须输入新对话的名称（/name）,输出完成后，回车关闭这个插件，并且进入到聊天界面
v: 暂定
i: 弹出对话框展示 Session Info 信息，就是 /seesion 中的内容（这个对话框弹出的时候使用y，可以复制对话框中全部的内容）

### 左侧列表第二个面板(tree)

~~/: 底部出现 搜索：两个字样，随后用户输入内容回车进行搜索，只搜索当前fouce的,文本需要高亮，使用 n/N 可以上一个和下一个，具体参考lazygit的形式~~（2026-09-22 曾改为只在 a 打开的树对话框里搜；同日又恢复；2026-09-23 完成：和对话框共用 `matchTreeRow`——每个词都要出现在 label / 正文里，`tag:` 只看 label，`role:` 只看角色，`after:` / `before:` 看时间（role 不再参与普通词匹配，和 pi /tree 不同）；折叠段里的行也算匹配，跳过去时展开它的祖先，右侧 CONTENT 跟着高亮；对话框里的搜索仍是过滤，两边互不影响）
~~j/k/方向上下键盘：上下移动~~

- ~~光标变化时右侧 CONTENT 跟着定位：对应消息框头部显示 `›` 箭头并加选中背景，同时把这条消息滚到面板顶部；节点在别的分支上时右侧改为显示“以该节点为叶子”的分支~~
- ~~树线：左侧画 pi `/tree` 一样的树形线条（`├⊟` / `└─` / `│`，⊟ 表示有子节点），活动分支上的节点文字前加 `• `，system 提示词 / compaction / branch summary 显示为 `[system]` / `[branch summary]: …` 这种标签行；层级最多画 3 层，更深的折叠成 `… `；完整的树在 a 打开的对话框里看~~（2026-09-22 起小面板改成折叠大纲：段头左侧画 `▸` / `▾` 三角、没有后代的旁支画 `─`，旁支默认折叠、活动分支展开，段内每层缩进 2 列、最多四层、更深的以 `… ` 代替，线性对话不缩进；pi 风格的树线只保留在 a 打开的对话框里）
- ~~z: 折叠 / 展开光标所在的分支段：光标在段头（`▸` / `▾` 行）上切换；在段内任意一行按下则折叠所在段并把光标移到段头（vim 的 zc）；线性对话的主干上没有可折叠的段，footer 提示 nothing to fold here~~
~~y: 复制消息内容（类似于 /tree 里面 ctrl + x 快捷键的功能）~~
~~T: 给某个节点添加 label，在面板中央弹出一个输入框（类似 lazygit commit 的弹窗；和 /tree 里面 shift+t 快捷键一致；不用 l 是为了把 l 留给全局的“下一个面板”），打标签之后 / 搜索也要能按照标签搜索~~（2026-09-22 起标签在树对话框的搜索里能搜到：普通词就匹配 label，`tag:xxx` 只看 label）
~~d/t/u/L/a: 过滤（过滤 类似 /tree ：filters ctrl+d/t/u/l/a 快捷键的功能，其中“只看有标签”用大写 L）~~（移到树对话框里，小面板只显示部分数据，过滤没意义；对话框里 labeled 改用小写 l）
~~a: 打开树对话框：顶部搜索框、中间完整的树形结构（不折叠层级）、底部快捷键提示，占满终端只留 2 列 / 1 行边距，光标停在面板当前节点上；Esc/q 关闭~~（对话框和面板共用折叠状态，折叠的段头在树线上画 `⊞`。2026-09-22 起对话框内的键按 `tree-dialog` → `tree` → `global` 的顺序解析：j/k/gg/G 移动自己的光标，y / T / Enter 和面板一样但作用于对话框选中的行（Label 弹窗、Summarize 菜单画在对话框上面），z 折叠，d/t/u/l/a 过滤（t/u/l/a 再按一次回 default；重新加载树、清空折叠，面板同步显示同一过滤并在标题里标出），`/` 聚焦顶部搜索框实时过滤（每个词都要出现在 label / 正文里，`tag:` 只看 label，`role:` 只看角色，`after:` / `before:` 看时间；2026-09-23 起 role 不再参与普通词匹配；Esc 退出搜索框、关键字和结果保留，再按 / 接着改、删光即清除，Enter 在搜索框里没有含义；搜索期间折叠全部打开、删光或关对话框后恢复），列表上 q / Esc 关闭并让面板光标跳到对话框选中的行——藏在折叠段里就展开它，右侧内容跟着高亮；对话框不做 ? 帮助，键都在底部一行）
~~enter: 确定要恢复到这个界面上，此时展示菜单，提供三个选项（No summary，Summarize，Summarize with custom prompt）~~（对应 /tree 的 restore：居中弹出三选菜单，j/k 移动、Enter 确认、Esc 退回 tree 面板；选 custom prompt 再弹一个单行输入框输入摘要指令，Esc 退回菜单；节点在其他会话里就先切会话再恢复；光标就在活动叶子上、或 pi 设置了 `branchSummary.skipPrompt` 时不弹菜单、直接进入）

三个选项只在一点上不同：跳到目标节点后，"被放弃的那段分支"（从当前叶子往上到它和目标节点的公共祖先之间的所有消息）怎么处理。跳转本身都一样：pi 把叶子指针移到目标节点，之后发给模型的上下文变成"根 → 目标节点"这条路径；被放弃的消息仍留在会话文件里，树里还能看到、以后还能跳回去。

- No summary：只移动叶子指针，不调模型、不花钱、瞬间完成，被放弃分支上发生过的事模型完全"忘记"。适合那段分支是死胡同，或只是想回到某句话重新问。
- Summarize：pi 把被放弃分支上的消息按时间顺序序列化成文本，用当前模型写一段结构化摘要（固定格式 Goal / Constraints & Preferences / Progress（Done、In Progress、Blocked）/ Key Decisions / Next Steps，要求保留精确的文件路径、函数名和报错信息），开头加一句"用户之前探索过另一条分支，以下是那次探索的总结"，结尾附上那段分支读过和改过的文件列表。结果作为一条 `branch_summary` 条目挂在目标节点下面成为新叶子，模型继续对话时知道另一条路试过什么、有什么结论。代价是一次模型调用（输出最多 4096 token，用量计入会话总费用），要等几秒到几十秒，且必须有模型可用，否则报 `No model available for summarization`。适合那段分支做过有价值的探索或修改，想保留结论但不想把整段对话的 token 都带回来。
- Summarize with custom prompt：和 Summarize 一样，只是把输入的指令追加到默认提示词后面作为 `Additional focus: …`，默认结构仍然保留。用来指定摘要侧重点，比如"只记录和 API 改动相关的决定"或"完整保留报错堆栈"。
- 补充：目标是用户消息时，pi 把叶子移到它的父节点并把那句话填回输入框让你改了重发（摘要挂在父节点位置）；目标在当前叶子的下游（没有任何消息被放弃）时，选 Summarize 也不会调模型，等同 No summary；分支太长放不进上下文时按 `branchSummary.reserveTokens`（默认 16384）预留空间、裁掉更早的消息再总结。摘要条目在 TREE 里显示为 `system: branch summary: …`，默认过滤下隐藏，按 `a` 可见。

### 右侧第一个面板（content 只读）

这个需要区分是“我”发送的内容，还是AI返回的内容
感觉这部分不需要这么重度的操作，用户大概率只需要粗略的查看这里面的内容，所以感觉快捷键只需要：
~~j/k/方向键：用来上下滚动查看内容~~
~~y: 复制对应对话的内容直接上tree面板复制就行了，不必要在这里~~
~~gg: 回到顶部~~
~~G：回到底部~~
~~/: 底部出现 搜索：两个字样，随后用户输入内容回车进行搜索，只搜索当前fouce的,文本需要高亮，使用 n/N 可以上一个和下一个，具体参考lazygit的形式~~（2026-09-23 完成：按渲染后的正文行匹配自由文本（不支持限定词，消息框的头部不参与），跳转把该行滚到面板顶部，命中文字在缓存的排版行上叠加高亮）

~~/: 底部出现 搜索：两个字样，随后用户输入内容回车进行搜索，只搜索当前fouce的,文本需要高亮，使用 n/N 可以上一个和下一个，具体参考lazygit的形式
h/j/k/l/键盘方向键: 光标移动
v: 预览模式
y: 复制内容
yy: 复制一整行
b/e: 跳过单词
gg: 回到顶部
G：回到底部
zz: 滚动到中心
其实就是 vim 这些快速跳转的快捷键, 把常规常用的全部加上就行~~

## 使用语言

- typescript
- node.js

## 注意点

1. 搜索可以强化一下语义，可以类似于github那样：auther:xxxx 这个代表找到作者是 xxxx 的仓库，这里的顶部搜索也可以参考这种设计，例如：model:xxx 当然这个根据实际情况考虑，如果做不到就算了（我建议第一版不要做真正的 semantic search。但是可以先设计成：
/name:FilmRecall
/model:opus
/path:FilmRecall
/tag:scan
/after:2026-09-01
/before:2026-09-20）（2026-09-23：已按这套语法实现，另加 `role:`——普通词只搜 名称 / 标题 / label，模型、路径、角色、时间都要加限定词：SESSIONS 认 name: / model: / path: / after: / before:，TREE 和树对话框认 tag: / role: / after: / before:，CONTENT 只搜自由文本；不认识的 key 和解析不了的日期当普通词搜，某个面板没有的字段对应的限定词被忽略；见 docs/keybindings.md 的 Search 一节）
2. 快捷键可配置，最好能够叫用户自定义快捷键，上面不是最优解, 可以通过配置文件或者在tui修改的形式来修改快捷键，并且要允许组合建，比如用户希望使用`ctrl + d` 来删除对话，这个也是可以的
3. 每次fork和delete操作的时候都需要，弹出一个对话框让用户来手动确定。（上下结构，确认和取消（y/n））
4. 右侧聊天内容要想办法区分“我”的内容和“AI”发送的内容，并且使用 markdown 渲染，最好直接用 直接用pi原生的渲染器
5. 确定左侧 resume 面板里面展示的内容，比如聊天的时间，模型，名称，运行路径，聊天内容的简介？等等不一定要全部展示，主要还是要UI美观，tree 面板就非常简单了，就按照

```txt
  user: hi
› [我的] 22:18 assistant: Hi! 👋 I'm ready to help with your **FilmRecall** Android project — whether that's writing or refactoring code, de
```

前面 `user` 代表是用户 `assistant` 代表是AI，`：`后面是内容, `[]` 里面是标签, `22:18` 是时间。

## UI 图

```

                 25%                  75%
          ┌───────────────┬─────────────────────────────┐
          │               │                             │
          │ [1] SESSIONS  │                             │
          │               │                             │
          │               │        [3] CONTENT          │
          │               │                             │
          │               │                             │
          ├───────────────┤                             │
          │               │                             │
          │   [2] TREE    │                             │
          │               │                             │
          │               │                             │
          └───────────────┴─────────────────────────────┘

┌───────────────────┬────────────────────────────────────────┐
│                   │                                        │
│     SESSIONS      │                                        │
│                   │                                        │
│   选对话           │                                        │
│                   │                                        │
├───────────────────┤              CONTENT                   │
│                   │                                        │
│       TREE        │              看内容                     │
│                   │                                        │
│   选节点           │                                        │
│                   │                                        │
└───────────────────┴────────────────────────────────────────┘

┌──────────────────────────────────────┐
│           SESSION INFO               │
│                                      │
│ Name       FilmRecall                │
│ Model      claude-opus-4             │
│ Messages   128                       │
│ Tokens     84.2k                     │
│ Cost       $1.42                     │
│ Created    Sep 20 22:18              │
│ Updated    Sep 20 22:21              │
│ Path       ~/.pi/sessions/...        │
│                                      │
│                         [Esc] Close  │
└──────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│ CONTENT                                                                  │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─ YOU · 22:18 ─────────────────────────────────────────────────────┐   │
│  │                                                                   │   │
│  │  你帮我设计一下 FilmRecall 的 Android 项目目录结构。              │   │
│  │                                                                   │   │
│  └───────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌─ ASSISTANT · 22:18 ────────────────────────────────────────────────┐   │
│  │                                                                   │   │
│  │  可以。                                                           │   │
│  │                                                                   │   │
│  │  我建议把扫描模块拆成几个部分：                                   │   │
│  │                                                                   │   │
│  │  ### 1. Scanner                                                  │   │
│  │                                                                   │   │
│  │  负责 CameraX 相机预览以及拍摄。                                  │   │
│  │                                                                   │   │
│  │  ### 2. Film Boundary Detection                                  │   │
│  │                                                                   │   │
│  │  使用 OpenCV 检测胶片边界……                                      │   │
│  │                                                                   │   │
│  └───────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│ NORMAL │ / Search   ? Help   l Focus   Enter Restore   y Copy   gg G    │
└──────────────────────────────────────────────────────────────────────────┘

```
